*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Build-Ordner jedes Moduls. Bisher stand hier nur "/build" fuer die
# Wurzel, den Rest erledigten die .gitignore-Dateien der Module — ein
# neues Modul ohne eigene Datei hat seinen kompletten build/-Ordner
# eingecheckt. Das Muster hier greift auf jeder Ebene.
build/
# Sitzungs- und Fehlerdateien des Kotlin-Compiler-Daemons
/.kotlin

# Claude-Code-Arbeitsdaten (Agent-Worktrees etc.)
/.claude/

# iOS: Das Xcode-Projekt wird aus ios/project.yml generiert (XcodeGen) und
# gehoert deshalb NICHT ins Repo — eine eingecheckte Kopie wuerde still
# gegen die Definition auseinanderlaufen.
/ios/*.xcodeproj
/ios/*.xcworkspace
/ios/build/
/ios/DerivedData/
xcuserdata/
*.xcuserstate
.swiftpm/

# Web: Playwright & Co. fuer die Screenshot-Tests (die PWA selbst hat
# bewusst kein Build-Tooling).
node_modules/
/web/node_modules/
/web/package-lock.json
